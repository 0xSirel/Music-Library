apiVersion: v1
kind: Service
metadata:
  name: {{ include "music-library-chart.fullname" . }}
  labels:
    {{- include "music-library-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "music-library-chart.selectorLabels" . | nindent 4 }}
