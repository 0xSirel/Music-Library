apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: {{ .Values.namespace }}
  name: {{ .Values.deployment.name }}
  labels:
    app: {{ .Values.appLabel }}
spec:
  selector:
    matchLabels:
      app: {{ .Values.appLabel }}
  template:
    metadata:
      labels:
        app: {{ .Values.appLabel }}
    spec:
      affinity:
        podAntiAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
          - labelSelector:
              matchExpressions:
              - key: app
                operator: In
                values:
                - {{ .Values.appLabel }}
            topologyKey: "kubernetes.io/hostname"

      securityContext:
        runAsNonRoot: true

      dnsPolicy: "None"
      dnsConfig:
        nameservers:
          - 8.8.8.8
          - 8.8.4.4
        searches:
          - music-library.svc.cluster.local
          - svc.cluster.local
          - cluster.local

      containers:
      - name: {{ .Values.appLabel }}
        securityContext:
          runAsUser: 10001
          runAsGroup: 10001
          allowPrivilegeEscalation: false
          capabilities:
            drop:
              - ALL
          seccompProfile:
            type: RuntimeDefault
        image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
        imagePullPolicy: {{ .Values.image.pullPolicy }}
        ports:
          - containerPort: {{ .Values.deployment.containerPort }}

        livenessProbe:
          httpGet:
            path: /api/health_check
            port: 5002
          initialDelaySeconds: 30
          periodSeconds: 10
          failureThreshold: 3
        readinessProbe:
          httpGet:
            path: /api/health_check
            port: 5002
          initialDelaySeconds: 10
          periodSeconds: 5
          failureThreshold: 3

        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
            ephemeral-storage: "2Gi"
          limits:
            memory: "512Mi"
            cpu: "500m"
            ephemeral-storage: "4Gi"

        envFrom:
          - configMapRef:
              name: music-library-env
          - secretRef:
              name: music-library-secrets
